import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.presidio.demo',
  appName: 'Presidio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
